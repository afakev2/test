services:
  - type: web
    name: discord-selfbot
    runtime: docker
    repo: https://github.com/YOUR_USERNAME/YOUR_REPO
    plan: free
    region: frankfurt
    envVars:
      - key: DISCORD_TOKEN
        sync: false
        required: true
    healthCheckPath: /health
    autoDeploy: true
